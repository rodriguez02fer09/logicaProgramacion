// dado un numero mostrar todos   los numeros desde  ese  al 0 de 8 en 8 
//en una lista con guiones donde cada numero debe empezar por n
const listarNumerosHastaCero = (numero) => {
    let resultado = `---DEL ${numero} al 0 --- \n`
        for (let i = numero ; i >= 0 ; i--){
            i
        if (i % 8 === 0) {
            i
            resultado += `-nº ${i}\n`
        }
        if(i === 0) {
         i
         resultado += "----FIN----"
         return resultado
        }
      }
    }
    c = listarNumerosHastaCero(8)
    c
    